<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to "Bloggers", users_path, class: "nav-link" %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Artilces
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <%= link_to "See articles", articles_path, class: "dropdown-item" %>
            <% if logged_in? %>
              <%= link_to "Create article", new_article_path, class: "dropdown-item" %>
            <% end %>
          </ul>
        </div>
        <% if logged_in? %>
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= "(Admin)" if current_user.admin? %> Profile [ <%= current_user.username %> ]
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item" %>
              <%= link_to "Delete your account", user_path(current_user), class: "text-danger dropdown-item", method: :delete, data: { confirm: "Are you sure?" } %> 
            </ul>
          </div>
        <% end %>
        <%= link_to "About", about_path, class: "nav-link" %>
        <% if logged_in? %>
          <%= link_to "Log out", logout_path, class: "nav-link", method: :delete, data: { confirm: "Are you sure?"} %>
        <% else %>
          <%= link_to "Log in", login_path, class: "nav-link" %>
          <%= link_to "Sign up", signup_path, class: "nav-link" %>
        <%end%>
      </div>
    </div>
  </div>
</nav>